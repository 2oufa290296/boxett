class NotificationsClass {
  String notId, notPP, notName, notText, notType;
  String notDate;

  NotificationsClass(this.notId, this.notPP, this.notName, this.notText,
      this.notType, this.notDate);
}
