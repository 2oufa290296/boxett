class EditReviews {
  String giftId, profilepic, review, orderId;
  double rate, onTime, goodPackaging, materialQuality, worthPrice;

  EditReviews(this.giftId, this.profilepic, this.review, this.rate, this.onTime,
      this.goodPackaging, this.materialQuality, this.worthPrice,this.orderId);
}
