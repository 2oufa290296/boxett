class ChatClass {
  String  pageImage, pageName, lastMessage, messageDate,pageId;
  int unread;

  ChatClass(this.pageImage, this.pageName, this.lastMessage, this.messageDate,this.unread,this.pageId);
}
